% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{taxo_resolve}
\alias{taxo_resolve}
\title{Resolve taxon names in a batch with an auditable result}
\usage{
taxo_resolve(
  taxa,
  ambiguity = c("warn", "first", "error"),
  verbose = FALSE,
  progress = TRUE
)
}
\arguments{
\item{taxa}{A character vector of taxon names or numeric Taxonomicon IDs.}

\item{ambiguity}{How to handle names with more than one valid biological
candidate. \code{"warn"} (default) selects the first candidate and emits one
aggregate warning, \code{"first"} selects it silently, and \code{"error"} aborts
after all inputs have been inspected.}

\item{verbose}{Logical. If \code{TRUE}, prints retrieval messages.}

\item{progress}{Logical. If \code{TRUE}, displays a progress bar.}
}
\value{
A data frame of class \code{taxodist_resolution} with one row per input
and columns \code{input}, \code{resolved_name}, \code{id}, \code{status}, \code{n_candidates},
\code{lineage_depth}, \code{lineage}, and \code{candidates}. The last two are list
columns. Status is one of \code{"resolved"}, \code{"ambiguous"}, \code{"unresolved"},
or \code{"retrieval_error"}.
}
\description{
Resolves taxon names or numeric Taxonomicon IDs while preserving the
candidates considered, the selected identifier, the retrieved lineage, and
the resolution status for every input. This is intended for reproducible
workflows in which warnings emitted during a large matrix calculation would
otherwise be difficult to audit afterwards.
}
\details{
Ambiguous inputs retain \code{status = "ambiguous"} even when the first candidate
is selected. Consequently, downstream code can distinguish an unambiguous
match from a match chosen according to the requested policy. Numeric IDs
bypass name search and are treated as single candidates when their lineage
can be retrieved.

Objects returned by \code{taxo_resolve()} can be passed directly to
\code{\link[=distance_matrix]{distance_matrix()}}, which reuses the stored lineages and preserves the
original input names as matrix labels.
}
\examples{
\donttest{
resolved <- taxo_resolve(c("Tyrannosaurus", "Nereis", "50841"))
distance_matrix(resolved)
}
}
\seealso{
\code{\link[=taxo_search]{taxo_search()}}, \code{\link[=distance_matrix]{distance_matrix()}}, \code{\link[=check_coverage]{check_coverage()}}
}
