% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{taxo_from_lineages}
\alias{taxo_from_lineages}
\title{Create an auditable resolution from user-supplied lineages}
\usage{
taxo_from_lineages(lineages, ids = NULL, source = "user-supplied")
}
\arguments{
\item{lineages}{A named list of character vectors ordered from root to the
focal taxon. List names become the input labels.}

\item{ids}{Optional character vector of unique identifiers, in the same
order as \code{lineages}. A named vector is matched by name. If omitted,
deterministic identifiers of the form \verb{custom:<input label>} are created.}

\item{source}{A non-empty character string describing the lineage source.}
}
\value{
A \code{taxodist_resolution} object that can be passed to
\code{\link[=distance_matrix]{distance_matrix()}} or \code{\link[=taxo_bundle]{taxo_bundle()}}.
}
\description{
Builds a \code{taxodist_resolution} object without consulting an online
taxonomy service. This is useful for unpublished classifications, curated
local taxonomies, frozen analyses, and fully offline workflows.
}
\examples{
lineages <- list(
  Alpha = c("Biota", "Animalia", "Alpha"),
  Beta = c("Biota", "Animalia", "Beta")
)
resolved <- taxo_from_lineages(lineages)
distance_matrix(resolved)
}
\seealso{
\code{\link[=taxo_resolve]{taxo_resolve()}}, \code{\link[=distance_matrix]{distance_matrix()}}, \code{\link[=taxo_bundle]{taxo_bundle()}}
}
